name: Sync and PR on push to test branch

on:
  push:
    branches:
      - '*'

jobs:
  sync_and_pr:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          ref: main
          fetch-depth: 0

      - name: Merge and push to test branch
        run: |
          git checkout test
          git merge main --allow-unrelated-histories
          git push origin test
